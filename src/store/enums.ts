export enum RootGetters {
  IS_MOBILE_DEVICE = "IS_MOBILE_DEVICE",
}
export enum RootMutations {
  IS_MOBILE_DEVICE = "IS_MOBILE_DEVICE",
}
export enum RootActions {
  IS_MOBILE_DEVICE = "IS_MOBILE_DEVICE",
}

export enum CardsGetters {
  ALL_CARDS = "ALL_CARDS",
}
export enum CardsMutations {
  ADD_CARD = "ADD_CARD",
  SET_FREEZE = "SET_FREEZE",
  MARK_CANCEL = "MARK_CANCEL",
}
export enum CardsActions {
  ADD_CARD = "ADD_CARD",
  SET_FREEZE = "SET_FREEZE",
  MARK_CANCEL = "MARK_CANCEL",
}
